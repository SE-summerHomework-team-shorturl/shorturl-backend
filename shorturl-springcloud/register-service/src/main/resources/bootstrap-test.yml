pring:
  cloud:
    config:
      name: sql
      profile: test
      label: master
      discovery:
        enabled: true
        service-id: config-server
  application:
    name: register-service # 应用名称
