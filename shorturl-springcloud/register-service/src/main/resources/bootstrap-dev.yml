
spring:
  cloud:
    config:
      name: sql
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: config-server
  application:
    name: redirect-service # 应用名称
