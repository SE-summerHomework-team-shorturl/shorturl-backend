server:
  port: 8011

spring:
  application:
    name: addurl-service # 应用名称
  cloud:
    config:
      name: sql
      profile: prod
      label: master
      discovery:
        enabled: true
        service-id: config-server
