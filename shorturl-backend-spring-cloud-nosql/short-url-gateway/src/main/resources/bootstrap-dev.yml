spring:
  application:
    name: gateway
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        register: false
    config:
      discovery:
        enabled: true
        service-id: config
