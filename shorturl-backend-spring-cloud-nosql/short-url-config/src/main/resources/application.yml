server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    consul:
      host: localhost
      port: 8500
    config:
      server:
        git:
          uri: file:///C://Users/dell/Desktop/config
          search-paths: config-dev-sjc
          default-label: master
