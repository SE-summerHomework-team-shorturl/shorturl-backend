server:
  port: 8011
spring:
  application:
    name: shortener
  cloud:
    consul:
      host: localhost
      port: 8500
    config:
      discovery:
        enabled: true
        service-id: config
